name: Release dotnet
on:
  workflow_call:
    secrets:
      token:
        required: true
env:
  GH_TOKEN: ${{ secrets.token }}
  NPM_TOKEN: ${{ secrets.token }}
  NODE_AUTH_TOKEN: ${{ secrets.token }}
jobs:
  release:
    name: Release
    runs-on: self-hosted
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          persist-credentials: false
          path: /deployment/dotnet-erp.logistic/project

      - name: Set repository name
        run: echo "REPO_NAME=$(echo ${{ github.repository }} | cut -d '/' -f 2)" >> $GITHUB_ENV

      - name: Setup .NET
        run: |
          wget https://dot.net/v1/dotnet-install.sh -O dotnet-install.sh
          bash dotnet-install.sh --version 6.0.423 --install-dir $HOME/.dotnet
          export PATH=$HOME/.dotnet:$PATH

      - name: Verify project file exists
        run: ls -la /deployment/dotnet-erp.logistic/Erp.Logistic/Erp.Logistic.csproj

      - name: Stop service
        run: sudo pm2 stop Erp.Logistic || true

      - name: Create release
        run: dotnet publish /deployment/dotnet-erp.logistic/Erp.Logistic/Erp.Logistic.csproj -c Release -o /deployment/dotnet-erp.logistic/dist -r linux-x64 --self-contained false

      - name: Set execute permission on start script
        run: chmod +x /deployment/dotnet-erp.logistic/dist/start

      - name: Start service
        run: sudo pm2 start /deployment/dotnet-erp.logistic/dist/start --name Erp.Logistic
